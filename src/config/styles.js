import { StyleSheet } from 'aphrodite';

export const commonStyles = StyleSheet.create({
  hMax: {
    height: '100%',
  },
  wMax: {
    width: '100%',
  },
  max: {
    height: '100%',
    width: '100%',
  },
  scrollAble: {
    overflowX: 'hidden',
    overflowY: 'scroll',
  }
});
